// twoSum([6, 5, 3, 4, 1, 2], 7); // [ 2, 3 ]

// return indices of two integers that add to target int

// iterate over 

// # iterate over list 
// # see if current int subtracted from target equals any other int
// # n = len(nums)
// # n has to increment independently of i (use range?)
// # if nums[i] + nums[i + n] == target, return indexes of i and n

// function twoSum(nums, target) {

//     for (i = 0; i < nums.length, i += 1) {
//         if (target - 1 == )
//     }
//         if target - i == i+n:
//             return list(index(i), index(n))
// }